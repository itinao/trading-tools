# apps/dashboard/.env.local にコピーして使う（.env.local は gitignore）
# Vite の Host 検査で許可する名前。マシン名（.local）と .ts.net は自動で許可されるので、
# Tailscale の MagicDNS の短い名前などマシン名と違うものをカンマ区切りで書く
ALLOWED_HOSTS=my-machine
# 待ち受け（既定 0.0.0.0:3000）
# HOST=0.0.0.0
# PORT=3000
